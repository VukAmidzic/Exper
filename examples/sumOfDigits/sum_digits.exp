/*  This script prints the sum of 
 *  digits of given number
*/

number := 0;
scan(number);

sum := 0;

while (number /= 0) {
    number_mod := number % 10;
    sum := sum + number_mod;
    
    number := number / 10;
};

print(sum);
