/* This script demonstrates
 * defining a function and 
 * using it (here we calculate
 * factorial of given number) 
*/

def fact(n) :: {
    if (n = 1) {ret 1;}
    else {
        res := fact(n-1) * n;
        ret res;
    };
};

n := 0;
scan(n);

print(fact(n));
